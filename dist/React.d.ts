import { Component, Markup, Props } from "metalui";
declare const _default: {
    createElement: (tag: string | Component, props: Props | undefined, ...children: Markup[] | Markup[][]) => Markup;
};
export default _default;
